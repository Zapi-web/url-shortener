name: Main Pipeline

on:
    push:
        branches: [ main ]
        paths-ignore:
            - README.md
            - LICENSE
    pull_request:
        branches: [ main ]
        paths-ignore:
            - README.md
            - LICENSE

jobs:
    go-tests:
        uses: ./.github/workflows/go.yaml