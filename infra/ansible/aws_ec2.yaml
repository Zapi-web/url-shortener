plugin: amazon.aws.aws_ec2

regions:
  - eu-central-1

keyed_groups:
  - key: tags.Role
    prefix: ""
    separator: ""

hostnames:
  - ip-address

groups:
  server: "tags.Role == 'server'"

compose:
  ansible_host: public_ip_address
  private_ip: private_ip_address