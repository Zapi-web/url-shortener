services:
  go:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=db:6379

  db:
    image: redis:8.6-alpine
    volumes:
      - redis_data:/var/lib/redis/data

volumes:
  redis_data: